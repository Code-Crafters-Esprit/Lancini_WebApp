{% extends 'admin/index.html.twig' %}

{% block title %}<title>Create New User</title>
{% endblock %}

{% block body %}
	<div class="card">
		<div class="card-body">
			<h1>Create new User</h1>

			{{ include('user/_form.html.twig') }}

			<a class="btn btn-light" href="{{ path('app_user_index') }}">Back to list</a>
		</div>
	</div>
{% endblock %}
