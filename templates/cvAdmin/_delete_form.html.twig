<form method="post" action="{{ path('admin_cv_delete', {'idcv': cv.idcv}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cv.idcv) }}">
    <button class="btn">Delete</button>
</form>
