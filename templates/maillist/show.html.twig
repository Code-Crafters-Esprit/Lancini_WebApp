<!DOCTYPE html>

<title>Maillist</title>

{% block body %}
    <h1>Maillist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ maillist.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ maillist.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_maillist_index') }}">back to list</a>

    <a href="{{ path('app_maillist_edit', {'id': maillist.id}) }}">edit</a>

    {{ include('maillist/_delete_form.html.twig') }}
{% endblock %}
