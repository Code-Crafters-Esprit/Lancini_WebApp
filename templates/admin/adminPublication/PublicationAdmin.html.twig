{% extends 'admin/index.html.twig' %}








{% block body %}
<div class="header bg-primary pb-6">
<div class="container-fluid">
<div class="header-body">
<div class="row align-items-center py-4">
</div>
</div>
</div>
</div>



<div class="col" style =  "margin-top: -50px; ">
<div class="card bg-default shadow">
<div class="card-header bg-transparent border-0">
<h3 class="text-white mb-0">content list</h3>
 <div style="float: right" class="col-4 text-right">
                  <a href="{{path('creer_publications')}}"  class="btn btn-sm btn-primary">Add new content</a>
                </div>
</div>

<div class="table-responsive"   >
<table class="table align-items-center table-dark table-flush">
<thead class="thead-dark">
<tr>
<th scope="col" class="sort" data-sort="name">User</th>
<th scope="col" class="sort" data-sort="name">Title</th>
<th scope="col" class="sort" data-sort="name">Category </th>
<th scope="col" class="sort" data-sort="status">Update</th>
<th scope="col">delete</th>
<th scope="col"></th>
</tr>
</thead>
<tbody class="list">


{%for p in pub %}

<tr>
<td>{{p.proprietaire.Nom}} {{p.proprietaire.Prenom}}</td>  
<td>{{p.libelle}}</td>
<td>{{p.cat}}</td> 
<td>
<form method="GET" action="{{ path('modifierPublication')}}" style="display: inline-block;">
                            <input type="hidden" name="pub" value={{ p.idpub }}>
                            <button type="submit" class="btn btn-sm btn-outline-info btn-info  mr-4">Update</button>
                            </form>
                            </td> 
{# <td> <a class="btn btn-sm btn-outline-info btn-info  mr-4 "href="{{ path('modifierPublication',{'idpub': p.idpub})}}"> update</a>  </td> #}
<td> <a class="btn  btn-sm btn btn-outline-danger btn-info  mr-4 "  href="{{ path('supprimerPublication',{'idpub': p.idpub})}}"> delete</a> </td>
</tr>
{% endfor %}

</tbody>
</table>
</div>
</div>
</div>


{% endblock %}















